# Build output
/bin/
*.exe

# Coverage
coverage.out
coverage.html

# Local environment — GitHub App private keys and webhook secrets must never be committed
.env
.env.local
*.pem

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Local scratch space for trying the CLI by hand
test_bad_code/

# Local Claude Code state — session-specific permissions, not project configuration
.claude/settings.local.json
